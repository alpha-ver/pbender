= form_for @file do |f|
  - if @file.errors.any?
    #error_explanation
      %h2= "#{pluralize(@file.errors.count, "error")} prohibited this file from being saved:"
      %ul
        - @file.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
